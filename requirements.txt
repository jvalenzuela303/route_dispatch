# FastAPI and Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Data Validation and Settings
# pydantic>=2.9 required for Python 3.14 (pydantic-core wheel available from 2.9+)
pydantic[email]>=2.9
pydantic-settings>=2.4

# Database
# sqlalchemy>=2.0.36 required for Python 3.14 compatibility
sqlalchemy>=2.0.36
alembic==1.12.1
# psycopg2-binary>=2.9.11 required for Python 3.14 prebuilt wheel
psycopg2-binary>=2.9.11
geoalchemy2==0.14.2

# Redis Cache
redis==5.0.1

# Utilities
python-dotenv==1.0.0

# Password Hashing and JWT
bcrypt==4.1.2
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# ERD Generation (opcional - requiere gcc y graphviz-devel para compilar)
# Instalar manualmente si se necesita: sudo dnf install gcc graphviz-devel && pip install eralchemy2==1.3.8
# eralchemy2==1.3.8

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1

# Additional useful dependencies
httpx==0.25.2
python-multipart==0.0.6

# Geocoding
requests==2.31.0

# Route Optimization
# ortools>=9.10 required for Python 3.14 (9.8 not available)
# numpy>=2.0 required for Python 3.14 (1.26 has no cp314 wheel)
ortools>=9.10
numpy>=2.0

# Image processing for delivery photo evidence (resize before storage)
Pillow>=10.0.0
